Place model weights in this directory:

- `yolov8n.pt` for object detection.
- `dqn_drone_model.pth` for the RL policy (future use).

Do not commit large model files to source control unless required.
